{
  "name": "aramrandom-socketversion",
  "version": "0.1.0",

  "scripts": {
    "backend:dev": "cd backend && npm run dev",
    "frontend:dev": "cd frontend && npm start",
    "build": "rm -r backend/build && cd frontend && npm run build --prod && cp -r build ../backend"
  },
  "author": "Eirik Matthiasen",
  "license": "MIT",
  "dependencies": {}
}
